import * as window from './show_window.js';

export const intro_showwindow = () => {
    window.showWindow(window.i_window);
}

export const intro_hidewindow = () => {
    window.hideWindow(window.i_window);
}

export const pay_showwindow = () => {
    window.showWindow(window.p_window);
}

export const pay_hidewindow = () => {
    window.hideWindow(window.p_window);
}

export const sona_showwindow = () => {
    window.showWindow(window.s_window);
}

export const sona_hidewindow = () => {
    window.hideWindow(window.s_window);
}